IMPORTANT: This input is a PDF document, not a single image.
The PDF may contain ONE or MULTIPLE songs. Extract ALL distinct songs found.
- Each song should be a separate entry in the "songs" array.
- A single song that spans multiple pages is still ONE entry.
- If the PDF contains multiple different songs (e.g., a hymn book), create a separate entry for each.
